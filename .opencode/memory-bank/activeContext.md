# Active Context

## Güncel durum
Proje GitHub'dan indirildi (`C:\Users\faruk\Desktop\KatipCelebi-main`). Henüz bir git repo'su değil (ZIP olarak indirildi). Çalışma henüz başlamadı — uygulamanın çalışır durumda olduğu doğrulanmadı.

## En son yapılanlar
- Proje ZIP olarak indirildi ve `KatipCelebi-main/` klasörüne açıldı
- Git `C:\Program Files\Git\cmd` ile System PATH'te mevcut (terminal yeniden başlatınca aktif)
- Proje yapısı ve ana modüller incelendi (app.py, store/model dosyaları, storage, theme, texts, icons, openlibrary, covers, excel, tags, people model)

## Çalıştırma durumu
Python sanal ortamı kurulmadı, bağımlılıklar yüklenmedi, uygulama hiç çalıştırılmadı. `python src/app.py` henüz denenmedi.

## Bilinen sorunlar / riskler
- Test dosyası yok — değişiklikler elle doğrulanmalı
- Windows'ta adwaita-qt6 yok; Adwaita temaları Fusion'a düşer (beklenen davranış)
- PyQt6-Charts, materialyoucolor, keyring gibi bağımlılıklar ortamda kurulu değil

## Geliştirme öncelikleri / beklenen işler
(Bekleniyor — kullanıcı ne istediğini söyleyecek.)

## Kararlar
- Memory bank `.opencode/memory-bank/` konumunda tutulacak (kullanıcı seçimi)
- Çalışma dili Türkçe
